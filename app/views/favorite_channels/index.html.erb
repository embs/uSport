<% content_for(:include_js) { 'channels' } %>

<% content_for :left_side_bar do %>
  <%= render partial: 'shared/leftbar' %>
<% end %>

<% @favorites.each_with_index do |channel, counter| %>
  <%= render partial: 'channels/preview', collection: [channel] %>
<% end %>

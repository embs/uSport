<%= content_for :show_move_comments do %>
  <%= render partial: 'show.js.erb' %>
<% end %>
<table class="table table-striped">
  <tbody id='moves'>
    <%= render partial: 'move', collection: [@move],
      locals: { show_comment_button: nil } %>
  </tbody>
</table>
<%= link_to match_path(@move.match), class: 'btn' do %>
  <i class='icon-arrow-left'></i> Voltar para a Partida
<% end %>
<div id="disqus_thread"></div>

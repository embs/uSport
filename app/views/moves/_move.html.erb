<li id="<%=move.id%>" class="move cor">
  <div class="move-team-emblem">
    <%# Gambs temps %>
    <% if move.team.name == "Recife" %>
      <%= image_tag('escudos/1.gif') %>
    <% end %>
    <% if move.team.name == "Olinda" %>
      <%= image_tag('escudos/2.gif') %>
    <% end %>
  </div>
  <div>
    <div><%= move.to_string %></div>
    <p class="move-date"><%= l move.created_at, :format => :long %></p><br />
    <% if current_user %>
      <%= link_to 'Comentar lance',
        new_user_channel_match_move_comment_path(move.match.channel.owner, move.match.channel, move.match, move) %>
    <% end %>
    <%= link_to 'Ver comentários',
      user_channel_match_move_comments_path(move.match.channel.owner, move.match.channel, move.match, move) %>
  </div>
</li>

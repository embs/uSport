<%= form_for @channel, :url => { :action => "create" } do |f| %>
  <fieldset>
    <legend><%=t :new_channel %></legend>
    <%= f.text_field :name, :placeholder => "#{t :channels_name}" %><br />
    <%= f.submit nil, :class => 'btn' %>
  </fieldset>
<% end %>
